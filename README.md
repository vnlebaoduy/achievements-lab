# achievements-lab
Lab repo for learning GitHub PR and issue workflows

## Branch and pull request workflow

Changes land on `main` through a pull request from a topic branch:

1. Create a branch off `main`.
2. Commit the change on that branch and push it.
3. Open a pull request against `main`.
4. Merge once the change is ready.
